# 27. 3주 프로그램 Week 1 — Day 1 스크린 결산 (Track A/B/D 동시 판정)

2026-08-08. 계획 = `C:\Users\USER\.claude\plans\submit-ens9-physmix-zip-expressive-gizmo.md`
(TM-CommandNet 3주 프로그램, 사용자 승인). 게이트 = ENS-9 동결(`results/ens9/`), 산출물 `results/ens11/`.

**Day 1에 Week 1 세 트랙의 스크린이 전부 끝났다** (ens9/ens10 인프라 재사용 덕에 계획 대비 4일 단축).

## Track A — 매칭 v2: 라벨-프리 GO, **점수 트랙 kill** (D-49)

`sweep/trackman_v2.py` 신설 (v1 불변). 5레버 전부 구현: 재정규화 2회 루프 · 등판 튜플 집합
거리(유효 셀 481k) · KIA ×2 보정(115 투수-시즌) · app_q 5축 · τ=antimode 1.24.

- **라벨-프리 검증 전부 통과**: 손 극성비 399403×(등판 집합 축이 극성을 사실상 결정적으로 만듦) ·
  요일 perm z=26.0 p=0.0005 · 방법합치 92.0% · 팀 순도 70.7%(v1 69.5%) · **churn 0.5%**.
- **churn 0.5%가 핵심 발견**: v1 매핑은 이미 수렴 상태였다. v2 Tier1(281) ≈ v1 Tier1(407)의
  고신뢰 부분집합. 신규 자산 = `posterior.json`(792투수 후보 분포) · `matches_v2.csv`.
- **다운스트림 A/B (`tm_v2_refit.py`, physmix corr_pm 교체, 4폴드)**:

| 팔 | 커버(V24) | V24 | V23 | V22 | V21 | 판정 |
|---|---|---|---|---|---|---|
| v1(파리티) | 0.680 | +0.00 | +0.00 | +0.00 | +0.00 | 하네스 검증 ✓ |
| v2-hard (저margin 126쌍 제거) | 0.600 | −0.26 | +0.25 | −1.00 | −0.29 | ≈0 — 저신뢰 쌍은 무해했다 |
| v2-soft (posterior, 커버 0.78) | 0.784 | −1.10 | −3.80 | −0.01 | +0.50 | 추가 커버의 신호 < 잡음 |

⇒ Codex 사전등록 kill("다운스트림 +2 미만이면 점수 트랙 종료") 발동. **매칭 품질은 병목이
아니었다** — D-17의 "저신뢰 쌍 유해(−12)"는 트리 직삽 시절 이야기고, 보정기 운반에서는 중립.

## Track B — 보조라벨 공식-only MTL: **사전등록 kill 발동** (D-50)

`sweep/aux_mtl.py` 신설. factorized(성공 + 1[y=0] 실패유형 + 구종군) logit-skip student.
- 타깃 커버: 구종군 99.8% · 실패유형 19.3%(y=0 행 중 연속행).
- V24: main-only **−622**(δ가 in-sample 스킵 로짓의 잔차 노이즈를 추적 — 배관 결함),
  MTL −8.6, **MTL·위약 −26.3**.
- 판정: 공식-only는 kill(+2 미달 — 수렴 법칙 예측대로). **단 MTL−위약 = +17.7 분리 =
  보조라벨 감독 신호 자체는 실재.** W2 CommandNet(TM 표현으로 입력이 바뀌는 본셀)에서만 재사용.
- **W2 필수 교정: 스킵 로짓은 OOF/holdout으로** (in-sample 스킵 + 학습 가능 δ = 이중적합 붕괴).

## Track D — 조건부 물리: **+1~2급 실재, 결합 재료 보존** (D-51)

`sweep/tm_condphys.py` 신설. Δphys[p,g,count,hand] 7축(velo/drag/relh/ext/spin/ivb/hb,
κ_cell=30, career<s) × 행 갱신 EB mix 적분.

| w0.5 | V24 | V23 | V22 | V21 | 3폴드 평균 |
|---|---|---|---|---|---|
| full7(주셀) | +2.05 | +0.14 | +3.53 | −3.17 | +1.91 > SD 1.70 (통과) — 단 V21 음수 |
| core4(velo·drag·relh·ext) | +1.17 | +1.15 | +2.97 | **+2.37** | +1.76 > SD 1.04, **4폴드 전부 양수** |
| 위약(8셀) | 평균 ≈ +0.4 | | | | 분리 약함(V23·V22 일부 겹침) |

- velo 단독은 죽음(−0.14) — effort modulation의 원신호(0.38σ)는 실재하나 잔차 예측력은
  다축 결합에서만 나온다. **신규 미사용 컬럼(drag·relh·ext)이 값의 절반 이상.**
- 판정: 단독 채택선 미달(예상 범위) — **core4를 결합 재료로 보존** (dn+tilt +4.8과 동급 계열).

## 종합 — Day 1 이후의 정직한 지도

- 확보된 신규 결합 재료: **condphys core4 (+1.8급, 4폴드 양수)**. 기존 보유: dn+tilt(+4.8).
- 죽은 것: 매칭 개선 채널(수렴 확인) · 공식-only 보조라벨 MTL.
- **남은 본명 = W2 E1(TM 조건부 encoder + CommandNet)** — 입력 정보를 바꾸는 유일한 대형 트랙.
  E2(pitch-level 정렬)는 라벨-프리 GO를 받았으므로 시도 자격 유지(moonshot).
- 결합 산수(현재): ENS-9 + dn+tilt(+4.8) + condphys(+1.8, 직교 가정) ≈ +6~7 « +10.
  E1/E2에서 +4~8이 나와야 채택선이 사정권.

## 신규 코드
`trackman_v2.py`(v2 매칭+posterior) · `tm_v2_refit.py`(A/B) · `tm_condphys.py` ·
`aux_mtl.py` · `tm_physmix.build_group_profile(mmap=)` 파라미터 추가. 전부 커밋 대기.

---

# W2 조기 실행분 (같은 날 저녁, D-52~54)

## E1 — TM encoder: NN student 사망, **캐리어 교체로 +2급 생존** (`commandnet.py`)

- 인코더 라벨-프리 3회 반복: as-of 물리 7 + 믹스 3 기준선 주입 + fallback 임베딩 학습 →
  **구종군 CE +6.2% 통과** · 물리 MSE +2.6%(기준선은 이기나 5% 선 미달 — 변조 분산이 원래 그 크기).
- CommandNet NN student(OOF 스킵으로 배관 교정, 파리티 +0.00): **student 자체가 −4.8,
  주셀 −8.7 ≈ 위약 −9.6 → kill.** DL 캐리어 사망 패턴 재확인(9층째).
- **검증된 캐리어 교체**: corr+enc10(rCpm ridge) = **+2.26/+8.35/+1.07/+1.32**,
  위약 +0.35/−2.25/+3.82/+1.59 — V24·V23 분리, V22·V21 미분리. lin 직입은 불안정 사망.
  ⇒ enc10은 +2~4급 결합 재료 (V23 편중 주의).

## E2 — pitch-level 교차 정렬: **판정 변경 GO** (`pitch_align.py`, D-53)

- 등판 튜플 정확 대응 **29,043개(양측 유일)** · 투구 923,503쌍 정렬 · **상태열 일치 99.19%** ·
  커버 87.8%. 구종군 일치 94.2%로 사전등록 kill(99%) 형식 미달이나, **confusion이 단일 셀
  (복원 br → tm fb 2.3%, 컷패스트볼류 분류 차이)에 집중 + other 제외 대각 95.8%** —
  정렬 오류라면 주변분포 비례로 퍼졌어야 한다. **기준의 전제(동일 분류체계)가 틀렸음을 기록하고
  상태열 99.2% + 구조적 confusion으로 정렬 검증 통과 판정.** 인덱스 916k쌍 저장
  (`results/ens11/pitch_align_idx.npz` — go=false로 저장 안 됐으면 재실행 필요).
- 남은 실행: **물리→실패유형 반응 모델**(train 전용 학습, 서빙은 인코더 예측 물리로 적분) — 미구현.

## ENS-11 결합 dry-run — **+7.96 (w0.5), +10 미달이나 재료는 쌓인다** (`ens11_final.py`, D-54)

| w | V24 | V23 | V22 | V21 | 3폴드 평균 | SD |
|---|---|---|---|---|---|---|
| 0.25 | +5.36 | +6.78 | +2.78 | +0.15 | +4.97 | 2.03 |
| **0.5** | **+9.53** | **+10.99** | **+3.37** | **−2.45** | **+7.96** | **4.05** |
| 0.75 | +12.50 | +12.62 | +1.75 | −7.81 | +8.96 | 6.24 |

위약(w0.5) 평균: V24 −1.1 / V23 +2.6 / V22 −3.6 / V21 −4.6 — **전 폴드 분리**.
ablation(dn+tilt)은 ENS-10 측정 재현(+5.11/+8.51/+0.88/−0.33) — 하네스 일관성 ✓.
cp+enc 추가분 = 결합 기준 **+3.1**(4.83→7.96). V21 −2.45는 위약(−4.6)보다 양호 = 컬럼 노이즈 수준.

**판정: 미달, 미제출.** 투영 ΔLB ≈ +6.0 ± 8 (경신 확률 ~2/3 — ENS-10과 같은 함정 구간).
남은 경로: ①E2 반응 모델(+1~3 기대, enc10과 부분 중복) ②그래도 미달 시 사용자 보고 후 결정.

## E2 반응 모델 — 마지막 카드 +1급 확정 (`ens11_response.py`, D-55)

정렬 907,659쌍(물리 완전 904,788) · β 표본 폴드별 28~76만 · r_hat std 0.012.

| 증분(주셀4 대비, w0.5) | V24 | V23 | V22 | V21 | 평균 |
|---|---|---|---|---|---|
| +resp(β 동결 스칼라) | −0.70 | +3.13 | +0.48 | −1.5 | **+0.97** (위약βsh +0.06) |
| +encctx28(재적합) | +1.94 | +1.68 | −1.47 | **−7.8** | +0.72 — V21 붕괴, 기각 |

주셀4+resp 절대값: +8.83/+14.11/+3.85, V21 −3.92 — 평균 +8.93 > SD 5.14이나 **+10 미달 + V21 악화**.
경로 위약(전 블록 pid 교환)은 −0.1~−2.7로 완전 분리 — 신호는 전부 진짜, 크기가 이것뿐.

## 3주 프로그램 W1+W2 최종 결산 (D-55)

- **정직한 결합 상한 실측: 주셀4 w0.5 = +7.96 (SD 4.05, V21 −2.45) / 주셀5(+resp) = +8.93 (V21 −3.92).**
- 모든 신규 채널이 위약과 분리되는 **진짜 신호**였고, 전부 +1~3급 — 10층에 걸친 소진 지도의 연장.
- 투영: ΔLB ≈ +6.0~6.7 ± 8 → 경신 확률 ~2/3, 기대 기록갱신 ≈ +7 (LB는 최고점 유지).
- 결정(사용자): ①+8급 제출 ②미제출·Phase 3 전환 ③팀 블렌드 재개 대기.
