# 36 — T1.5 TM-표현 NN 레그: 하룻밤 구축·실측과 "d 벽"의 확정 (2026-08-29 심야)

> 라운드5(연구/14)의 T1 킬 이후 결정 트리대로 T1.5를 구축·측정했다.
> 코드 `sweep/tm_repr_leg.py` · 산출물 `results/tm_repr/` · 재료 = pitch_align 916,049쌍
> (`results/ens11/pitch_align_idx.npz`, `--force-save`로 실체화) + commandnet 인코더(폴드별 재학습).

## 0. 결론 먼저

1. **레그 자체는 성립한다**: LegNet(선형 백본 + zero-init 델타 트렁크 + 인코더 22피처 +
   정렬물리·복원라벨 보조손실), V24 3시드 배깅 **로컬 825.5** (전이 LB ≈ 943).
   같은 피처의 단일 LGBM(teacher) 대비 **3폴드 전부 우위** (+180 / +256 / +139).
2. **정렬물리 보조채널은 3폴드 채택 기준을 통과하지 못했다**: legP−위약 = **+29.7(V24) /
   −103.8(V23 파단 폴드) / +4.8(V22)** — 평균 −23 < 0. 정상 레짐에선 실재(+30, ≈2.7σ)하나
   파단 레짐에서 역효과(시드 SD 117로 불안정화). 채택 규칙(평균>0 & 평균>SD) 위배.
3. **🚨 "d 벽"이 네 번째 측정으로 확정됐다**: 유능한 같은-데이터 모델의 정직한 d(D4평균)는
   **0.024~0.026에서 수렴한다** — tm3L 0.0238 · ysy_mlp 0.026 · legP 0.0244 · **legPnb(basin
   21컬럼 제거) 0.0244 = 피처 수술로 정확히 0을 산다**(점수만 −28). D-37 수렴 법칙의 재확인.
4. **따라서 1100은 현 우주(제공 데이터+TM) 안에서 산술적으로 닫혀 있다**: d≤0.026이면
   레그가 LB 1045여도 5레그 기대 ≈ 1075 상한. 정확 기대표(아래)의 6레그 최상 셀도 1073.3.

## 1. 구축 기록 (재현용)

- 워크스페이스: `tm_repr_leg.py --fold {v} --stage prep` — XA(78 = X+is4 | bis | ebps | cxp,
  commandnet.gate() 조립 재사용) + 폴드 인코더(TM season<v) + 정렬물리(fit 커버 63.7~66.7%) +
  basin 제거 변형(Zc 163열) + june풍 LGBM teacher. 47~51s/폴드.
- 학습 레시피에서 잡은 결함 2건: ① CommandNet용 wd 3e-3을 선형 백본에 걸면 붕괴(−634) —
  **wd 분리**(백본 1e-4/트렁크 3e-3) ② 기저율 로짓 바이어스 초기화 + OneCycleLR 없이는
  수렴 실패(−1972) — nn_lin 검증 레시피 이식으로 해결. 이후 시드 SD 2.3까지 안정.
- 채점: `leg_score` = 합법 레거시 캘리(1.04, −0.01) 후 score. ⚠ **`eb_carrier.deploy_score`는
  금지된 역산 상수(slope 1.14202)를 적용**하므로 새 레그 게이트에 쓰면 안 되고(~88점 왜곡 실측),
  역사적 로컬 라인(805/834/877.8)과도 비교 불가.
- 사전 등록 킬 라인의 정정 기록: "3폴드 솔로<860"은 V24 전용 수치를 3폴드에 잘못 이식한
  것(V23 파단 폴드는 전 모델이 음수 — teacher −1810). 상대 게이트(vs teacher·vs 위약)로 판정.

## 2. 측정표 (V24 3시드, 레거시캘리 로컬)

| arm | solo 평균±SD | 배깅 | d(D4평균) | 비고 |
|---|---:|---:|---:|---|
| teacher(june풍 LGBM 단일) | 645.7 | — | — | 참조 |
| leg0 (인코더만, 물리 aux 없음) | 714.8±13.8 | 797.7 | 0.0273 | |
| **legP (주 설계)** | **799.3±2.3** | **825.5** | **0.0244** | 위약 +29.7 |
| legP_pl (위약: pid 교환+pid내 물리 셔플) | 769.6±0.9 | 802.7 | 0.0253 | |
| legPnb (basin 21컬럼 제거) | 768.4±10.1 | 797.6 | **0.0244** | **d 이동 0** |

폴드 확장: V23 legP −1554.7±117(teacher −1810.6, 위약 −1450.9) · V22 legP 2223.9±17.7
(teacher 2084.9, 위약 2219.1).

## 3. 정확 기대표 (캐시 예측 실측 이득, ρ=1.4295)

| 조합 | 프록시이득 | 기대 (legP {920/943/965} × mlp {910/939/960}) |
|---|---:|---|
| D4+legP (5) | 97.4 | 1057.5 / 1062.1 / 1066.5 |
| **D4+mlp (5) = blendE5** | 102.6 | 1059.1 / **1064.9** / 1069.1 |
| D4+mlp+legP (6) | 116.3 | 중심 셀(943,939) **1066.1** · 최상 셀(965,960) 1073.3 |

d(legP, mlp) = 0.0307 (상호 다양) — 그래도 6레그의 한계 이득은 E5 대비 **+1~4**뿐.
legP 패키징(반나절)+미지 점수 2개의 리스크 대비 얇다.

## 3b. blendE5 실측 (같은 밤 20:02) — mlp 사망, 표의 mlp 열 소거

**blendE5 = 1039.5281171389** → 역산 **S(ysy_mlp) = 5·(S₅−71.76)−4026.56 = 812.3 (±20)**.
자매 모델(937~940) 기반 사전확률이 125점 빗나감 — 함수족이 다르면 자매 점수는 prior가 못 된다.
§3 기대표의 mlp 시나리오 열은 전부 소거되고, 남는 후보는 **D4+legP (5레그)뿐**:
기대 = (4026.56+S_legP)/5 + 68.1, **손익분기 S_legP ≥ 925.8**, 전이 추정 943±8~12 →
기대 +0~4의 얇은 카드. 팀 최고는 blendD4 1058.6047851923 유지.

## 4. 결정

- **다음 제출 = blendE5 (준비 완료, 기대 ~1065, 손익분기 S(mlp) ≥ 907.6).** S(mlp) 역산 후
  6레그(F6) 승격 여부 재산정 — mlp ≥ 939 & legP 패키징 시 +1~4.
- **내일 30분 측정 1건 = T2의 운명 판정**: 전시즌(2019-23) 학습 june-LGBM의 d(D4평균).
  학습창 레짐 베팅은 ±400~750점급 예측 변화의 유일한 실증 원천 — 이것마저 d≤0.026이면
  **벽은 보편이고 1100 추구를 공식 종료**(목표를 "규정 준수 최대화 + Phase 3 품질"로 전환).
- legP는 채택 보류 상태로 보존(`results/tm_repr/pred_2024_*.npy` + 코드) — 정상 레짐 한정
  가치(+30)와 파단 취약성이 공존. Phase 3 서사(정렬 채널 개통·실측)로는 이미 가치가 있다.
